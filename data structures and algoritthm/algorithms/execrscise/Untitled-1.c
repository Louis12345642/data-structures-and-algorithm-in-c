#include <stdio.h>
#include <stdlib.h>
int main()
{
    int count = 3;
    printf("The initial value of count: %d \n \n", count); // output will be 3

    printf("Count will be increamented by one and then operation: %d \n \n", ++count);

    printf("Count will be printed first: %d \n \n", count++);

    /* Count will be printed first. Output is 4. Then count will be increamented by 1 to 5.*/

    printf("Count will be increamente by 1 to 5: %d", count); // out put will be 5
    #
}